{
	admin off
	auto_https off
}

:{$CADDY_PORT:8080} {
	encode gzip

	handle /api/* {
		reverse_proxy {$BACKEND_HOST:backend}:{$BACKEND_PORT:8000}
	}

	handle /* {
		reverse_proxy {$FRONTEND_HOST:frontend}:{$FRONTEND_PORT:3000}
	}

	log {
		output stdout
		format json
	}
}
